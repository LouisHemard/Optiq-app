services:
  db:
    image: postgres:16
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: optiq
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
